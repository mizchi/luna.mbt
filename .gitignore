# Build outputs
**/target
target/
dist/
!js/loader/dist/
dist-docs/
dist-demo/
.sol/
_build/
# astra test fixture builds (e.g. sol/astra/tests/fixture/.out/)
**/tests/fixture/.out/

# Wrangler / Cloudflare Workers
.wrangler/
node_modules/
.mooncakes/
# pnpm store left behind by docker bind-mount runs (e.g. seeding the
# Playwright VRT linux baseline through mcr.microsoft.com/playwright)
.pnpm-store/

# APM — agent skill manager (apm.yml + apm.lock.yaml are committed; modules cache + deployed skills regenerate via `apm install`)
apm_modules/
.agents/
# APM-installed skill deploy targets (regenerated by `apm install`, not tracked)
.claude/skills/apm-usage/
.claude/skills/ast-grep-practice/
.claude/skills/cloudflare-deploy/
.claude/skills/conventional-changelog/
.claude/skills/gh-fix-ci/
.claude/skills/justfile/
.claude/skills/moonbit-js-binding/
.claude/skills/moonbit-practice/
.claude/skills/playwright-test/
.claude/skills/retrospective-codify/

# IDE / OS
.DS_Store
.turbo/

# Test outputs
test-results/
coverage/
*.coverage
# NOTE: do NOT ignore __screenshots__ here. astra/examples/*/e2e/
# stores Playwright VRT baselines under __screenshots__/ that MUST be
# tracked. luna's local js/luna/.gitignore keeps its own
# tests/__screenshots__ scoped-ignored.
.test-output/

# Logs and databases
*.log
*.db

# Temporary
tmp/

# Local settings
.claude/settings.local.json

# TypeScript build info
*.tsbuildinfo
.claude/scheduled_tasks.lock
