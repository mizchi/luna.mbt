name: browser

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

jobs:
  # Vitest browser tests
  vitest-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: pnpm/action-setup@v4
        with:
          run_install: false
      - run: pnpm install
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: "pnpm"
      - uses: illusory0x0/setup-moonbit@v0.1.0
        with:
          version: stable
      - run: |
          moon update
          moon install
      - name: Build MoonBit
        run: |
          # Skip astra which depends on mooncakes markdown (API incompatibility)
          moon build --target js src/luna
          moon build --target js src/core/parser
          moon build --target js src/core/routes
          moon build --target js src/core/ssg
          moon build --target js src/platform/js/api
          moon build --target js src/platform/js/cache
          moon build --target js src/platform/js/fs_adapter
          moon build --target js src/platform/js/stream_renderer
          moon build --target js src/sol
          moon build --target js src/stella
      - name: Install Playwright Chromium
        run: npx playwright install chromium --with-deps
      - name: Vitest browser tests
        run: pnpm vitest run --config vitest.browser.config.ts

  # E2E tests with Playwright
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: pnpm/action-setup@v4
        with:
          run_install: false
      - run: pnpm install
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: "pnpm"
      - uses: illusory0x0/setup-moonbit@v0.1.0
        with:
          version: stable
      - run: |
          moon update
          moon install
      - name: Build MoonBit
        run: |
          # Skip astra which depends on mooncakes markdown (API incompatibility)
          moon build --target js src/luna
          moon build --target js src/core/parser
          moon build --target js src/core/routes
          moon build --target js src/core/ssg
          moon build --target js src/platform/js/api
          moon build --target js src/platform/js/cache
          moon build --target js src/platform/js/fs_adapter
          moon build --target js src/platform/js/stream_renderer
          moon build --target js src/sol
          moon build --target js src/stella
      - name: Build Loader
        run: pnpm exec rolldown -c rolldown.config.mjs
      - name: Install Playwright Chromium
        run: npx playwright install chromium --with-deps
      - name: E2E tests
        run: pnpm playwright test --config e2e/playwright.config.mts
