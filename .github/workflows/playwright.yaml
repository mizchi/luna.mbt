name: browser

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

jobs:
  # Vitest browser tests
  vitest-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: pnpm/action-setup@v4
        with:
          run_install: false
      - run: pnpm install
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: "pnpm"
      - uses: hustcer/setup-moonbit@v1
        with:
          version: latest
      - run: |
          moon update
          moon install
      - name: Build MoonBit
        run: |
          # Core libraries
          moon build --target js --release src
          moon build --target js --release src/js/api
          moon build --target js --release src/js/api_signals
          moon build --target js --release src/js/stream_renderer
          moon build --target js --release src/x/stella
          # Test packages for browser tests
          moon build --target js --release src/tests/counter_component
          moon build --target js --release src/tests/counter_client
          moon build --target js --release src/tests/browser_components
          moon build --target js --release src/tests/router_csr
          # Examples for browser tests
          moon build --target js --release src/examples/spa
          moon build --target js --release src/examples/todomvc
          moon build --target js --release src/examples/browser_router
          moon build --target js --release src/examples/wc
      - name: Install Playwright Chromium
        run: npx playwright install chromium --with-deps
      - name: Vitest browser tests
        run: pnpm vitest run --project browser

  # E2E tests with Playwright
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: pnpm/action-setup@v4
        with:
          run_install: false
      - run: pnpm install
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: "pnpm"
      - uses: hustcer/setup-moonbit@v1
        with:
          version: latest
      - run: |
          moon update
          moon install
      - name: Build MoonBit
        run: |
          # Core libraries
          moon build --target js --release src
          moon build --target js --release src/js/api
          moon build --target js --release src/js/api_signals
          moon build --target js --release src/js/stream_renderer
          moon build --target js --release src/x/stella
          # Test packages for e2e tests
          moon build --target js --release src/tests/counter_component
          moon build --target js --release src/tests/counter_client
          moon build --target js --release src/tests/browser_components
          moon build --target js --release src/tests/router_csr
          # Examples for e2e tests
          moon build --target js --release src/examples/spa
          moon build --target js --release src/examples/todomvc
          moon build --target js --release src/examples/browser_router
          moon build --target js --release src/examples/wc
          moon build --target js --release src/examples/apg-playground
      - name: Build Loader
        run: pnpm --filter @luna_ui/luna-loader build
      - name: Install Playwright Chromium
        run: npx playwright install chromium --with-deps
      - name: E2E tests
        run: pnpm playwright test --config e2e/playwright.config.mts
