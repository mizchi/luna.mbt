{
  "name": "@mizchi/astra-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd .. && moon build --target js --release",
    "pretest:e2e": "pnpm build",
    "test:e2e": "playwright test --config e2e/playwright.config.mts"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "memfs": "^4.72.2"
  }
}
